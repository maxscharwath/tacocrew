/**
 * Base application service
 * Application services orchestrate use cases and handle cross-cutting concerns
 * @module application/services
 */

/**
 * Base class for application services
 * Application services coordinate use cases and provide a high-level API
 */
export abstract class ApplicationService {
  // Can add common methods for logging, transaction management, etc.
}
