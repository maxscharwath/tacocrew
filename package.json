{
  "name": "tacobot",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.12.0",

  "devDependencies": {
    "@biomejs/biome": "2.3.2"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "dev:backend": "pnpm --filter @tacobot/backend dev",
    "dev:frontend": "pnpm --filter @tacobot/frontend dev",
    "test": "pnpm -r test",
    "check": "pnpm --filter @tacobot/backend check"
  }
}
